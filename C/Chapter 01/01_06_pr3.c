#include <stdio.h>

int main()
{
    // celcius to fahrenheit
    int celcius = 37;
    float far = (celcius * 9 / 5) + 32;
    printf("The value of this celsius temperature is %f", far);
    return 0;
}