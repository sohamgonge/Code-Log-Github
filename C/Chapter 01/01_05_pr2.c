#include <stdio.h>

int main()
{
    int radi = 3;
    float pi = 3.14;
    printf("The area of this circle is %f\n", pi * radi * radi);
    int height = 3;
    printf("Volume of this cylinder is %f", pi * radi * radi * height);
    return 0;
}