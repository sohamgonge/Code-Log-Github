#include <stdio.h>

int main()
{
    int a, b = a;
    // int a; b = a; invalid
    int v = 3 ^ 3; // this is valid
    printf("%d", v);
    // char dt = '21 dec 2020'; invalid cause only one character is allowed in char datatype
    return 0;
